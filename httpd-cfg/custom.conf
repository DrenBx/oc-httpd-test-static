<VirtualHost *:*>
    ServerName localhost

    Alias /files /var/www/static

    <Directory />
        Order Allow,Deny
        Allow From All
        Options -Indexes
    </Directory>

    <Directory /var/www/static>
        Options +Indexes
    </Directory>
</VirtualHost>
